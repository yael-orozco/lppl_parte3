int x; int y;

int main(){
    bool ok;
    ok = false;
    while (! ok ) {
        read(x); read(y);
        if (((x>=0) && (x<10)) && ((y>=0) && (y<10))) ok = true;
    else {}
    }

    return 0;
}